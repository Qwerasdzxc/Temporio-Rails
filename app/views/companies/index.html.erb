<h1>Companies</h1>

<% provide(:title, 'All companies') %>

<ul class="companies">
  <%= render @companies %>
</ul>

<br>

<% if current_user.admin?%>
  <%= link_to 'New Company', new_company_path %>
<% end %>